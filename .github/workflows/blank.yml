name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/setup-node@v1
    - name: npm install, build
      run: |
        npm install
        npm run build
        npm run rename
